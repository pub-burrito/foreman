start on starting <%= app %>-<%= name %>
stop on stopping <%= app %>-<%= name %>
respawn

env PORT=<%= port %>
<% engine.env.each do |name,value| -%>
env <%= name.upcase %>=<%= shell_quote(value) %>
<% end -%>

setuid <%= user %>

chdir <%= engine.root %>

exec <%= process.command %>
